# ER Improvement Journal
- Refactoring global state access using useShallow with explicit primitive selectors prevents unnecessary React component re-renders, especially when consuming frequently updated nested objects like a simulation state.
